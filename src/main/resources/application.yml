micronaut:
  application:
    name: oda-streamelements-service
  metrics:
    export:
      prometheus:
        enabled: true
        descriptions: true
        step: PT1M
    enabled: true
  serde:
    serialization:
      inclusion: ALWAYS
  security:
    authentication: bearer
    token:
      jwt:
        signatures:
          jwks:
            keycloak:
              url: ${JWKS_URI}
  executors:
    config-listener:
      type: fixed
      nThreads: 1
    event-listener:
      type: fixed
      nThreads: 1
endpoints:
  prometheus:
    sensitive: false
  metrics:
    sensitive: false
